import { EncodeBase64 } from './encode-base64';

describe('EncodeBase64', () => {
  it('should create an instance', () => {
    const directive = new EncodeBase64();
    expect(directive).toBeTruthy();
  });
});
