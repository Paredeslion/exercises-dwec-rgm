import { EncodeBase64Directive } from './encode-base64';

describe('EncodeBase64', () => {
  it('should create an instance', () => {
    const directive = new EncodeBase64Directive();
    expect(directive).toBeTruthy();
  });
});
